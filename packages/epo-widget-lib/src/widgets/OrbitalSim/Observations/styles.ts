import styled from "styled-components";

export const Label = styled.div`
    background-color: rgba(0, 0, 0, .75);
    width: 20px;
    text-align: center;
    border-radius: 15px;
    padding-top: -8px;
    font-size: 16px;
    color: var(--white);
    cursor: pointer;
    z-index: 0;
`;