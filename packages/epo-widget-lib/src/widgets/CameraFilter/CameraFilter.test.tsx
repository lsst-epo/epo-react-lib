import CameraFilter from ".";

describe("CameraFilter", () => {
  it.todo("displays a table of camera filters and their spectrum");
  it.todo(
    "displays an image of the electromagnetic spectrum from ultraviolet to infrared"
  );
  it.todo("has callouts for visible light colors and their spectrum");
  it.todo("has a dropdown with options for each camera filter");
  it.todo(
    "hides the spectrum of the unselected filters if a filter is selected"
  );
});
