const defaults = {
  width: 600,
  height: 600,
  xAxis: { step: 1, min: -1, max: 3 },
  yAxis: { step: 2, min: 22, max: 8 },
  pointRadius: {
    sm: 4,
    md: 6,
    lg: 8,
  },
};

export default defaults;
