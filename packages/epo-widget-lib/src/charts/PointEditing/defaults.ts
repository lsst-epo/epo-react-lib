const defaults = {
  /** Radius of the ghost point that follows the pointer */
  cursorPointRadius: 12,
  /** How much each repeated nudge grows the last one */
  nudgeAcceleration: 1.05,
  /** Milliseconds between repeats while a nudge button is held */
  nudgeInterval: 100,
};

export default defaults;
