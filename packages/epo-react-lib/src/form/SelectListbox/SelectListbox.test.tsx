import SelectListbox from ".";

describe("SelectListbox", () => {
  it.todo("Should display a placeholder if no value is select");
  it.todo("Should disable selection based on props");
  it.todo("Should open a dropdown list if clicked");
  it.todo("Should navigate the dropdown list options with arrow keys");
  it.todo(
    "Should close the dropdown list on selection, escape key, and click outside"
  );
  it.todo(
    "Should select an option on click, enter key, and space key and update the display"
  );
});
