{
  "private": true,
  "version": "0.4.0",
  "license": "MIT",
  "author": "Rubin EPO",
  "repository": {
    "type": "git",
    "url": "https://github.com/lsst-epo/epo-react-lib"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "build-storybook": "build-storybook",
    "deploy-storybook": "storybook-to-ghpages --packages=packages --out=.out",
    "test": "jest",
    "test:generate-output": "jest --json --outputFile=.jest-test-results.json || true"
  },
  "devDependencies": {
    "@storybook/storybook-deployer": "^2.8.16"
  }
}
